---
import { DesignGallery, SiteChrome } from '@trustfall/design';
import Base from '../layouts/Base.astro';

if (!import.meta.env.DEV) {
  return new Response('Not found', { status: 404 });
}
---

<Base title="Design gallery · TrustFall">
  <SiteChrome siteName="TrustFall">
    <DesignGallery />
  </SiteChrome>
</Base>
