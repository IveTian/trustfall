---
import { DesignGallery, SiteChrome, ThemeToggle } from '@trustfall/design';
import Base from '../layouts/Base.astro';

if (!import.meta.env.DEV) {
  return new Response('Not found', { status: 404 });
}
---

<Base title="Design gallery · TrustFall">
  <SiteChrome siteName="TrustFall">
    <ThemeToggle client:load />
    <DesignGallery />
  </SiteChrome>
</Base>
